export class Tune {
    id: number;
    title: string;
    abc: string;

    constructor(id: number, title: string, abc: string) {
        this.id = id;
        this.title = title;
        this.abc = abc;
    }
}
